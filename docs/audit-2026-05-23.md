# Audit 2026-05-23 — 보안 / 운영 / 재현성 백로그

Phase 6 완료 시점 스냅샷. OCI 실상태 + 로컬 infra 코드 + docs 3자 대조 결과. 향후 하나씩 해결할 항목 목록.

## 한줄 요약

네트워크 perimeter 자체는 docs 대로 깨끗. defense-in-depth 구멍 2개 + 운영 누락 1개 + Phase 3 이후 docs 재현성 깨짐.

## P1 — 보안 (defense-in-depth)

### ~~A. ops-vm `api-server` 포트 0.0.0.0 바인드~~ — 해결 (2026-05-24)

`docker-compose.yml` 의 `api-server.ports` 를 `${OPS_TAILNET_IP}:8080:8080` 로, ops-vm `.env` 에 tailnet IP 주입. OS 레벨에서 공인·사설·loopback 거부, tailnet 만 listen. NSG + OS bind 두 겹 방어. 검증: 사설 IP / loopback connection refused, tailnet IP HTTP 200, Caddy UI 정상 (docker 내부 네트워크 무영향), 3 worker polling 무중단.

### ~~B. lol-list PAT 평문 노출~~ — 해결 (2026-05-24)

ops·worker `lol-list` remote URL 에 PAT plaintext 임베디드 → SSH ed25519 deploy key 로 전환 완료. mac-server / ops-vm / worker-vm 셋 다 동일 패턴 (read-only deploy key per host). 잔여: 사용자 GitHub Settings → Personal access tokens 에서 노출됐던 PAT (`github_pat_11BK7JB3Y0JAOR...`) revoke.

### C. 공개 UI 의 SimpleAuthManager — rate limit / IP 화이트리스트 없음

`airflow.<your-domain>` 가 공개 HTTPS 로 노출, SimpleAuthManager 는 Apache 공식이 "dev/test only" 라 못박은 manager (`decisions.md` R3 에 인지됨). brute force 가능.

**옵션** (한 개만 골라도 충분):
- Caddy `@allowed { remote_ip <본인IP/32> }` — SSH 와 동일 정책, tailnet 은 항상 통과
- Caddy rate_limit module 로 `/login` 제한
- R3 트리거 — FabAuthManager 도입

minimal-disposable 철학에는 IP 화이트리스트가 자연스러움.

## P2 — 운영

### C. `airflow-logs` 볼륨 무한 증가

api-server·scheduler·dag-processor·edge-worker-ops 4개가 공유. 정리 정책 없음 → boot vol (125 GB) 시간 문제로 채워짐. docker log driver `max-size` / `max-file` 또는 Airflow 측 정책. Phase 8 모니터링 전이라도 필요.

### D. Caddy 보안 헤더 누락

HSTS, X-Frame-Options, X-Content-Type-Options 등 미설정. Caddyfile 한 블록 추가로 끝.

## ~~P3 — 재현성: docs 따라 처음부터 배포 → 현 상태 안 됨~~ — 해결 (2026-05-24)

setup.md 전면 갱신. Phase 2 fail2ban 제거, Phase 3 postgres:16 / edge-worker-ops / variables import / Caddyfile 현행화, Phase 4 lol-list + network_mode: host, Phase 5 Colima + Docker 방식, Phase 6 신설 (lol-list deploy key + airflow-variables.json).

## P4 — 잡 / 인지만 (액션 불필요)

- Tailscale ACL 기본값 = tailnet 전 포트 허용. 단일 사용자라 OK, device 추가 시 재고
- `simple_auth_manager_passwords.json` RW 바인드 — Airflow 가 거기에 비번 쓰는 구조, 불가피. gitignored 확인
- ops-nsg 22@본인IP/32 — IP 변동 시 갱신 잊지 말 것 (tailnet 으로 콘솔 복구 가능)
- host 측 `ufw` 미설정 — Tailscale-trusted 모델 + NSG 신뢰로 의식적 생략

## 참조

- 네트워크 / NSG / 서브넷 / 게이트웨이 / boot vol 실측치 일치: `docs/setup.md` Phase 1
- 결정 근거: `decisions.md` L10 / L11 / L20 / R3
- 다음 단계: 한 항목씩 별도 세션에서 해결
